/* The supervisor's responsibility is to assign jobs to workers and
to track of each worker's status on behalf of the manager. The server
is half of the client-server pattern needed to communicate over a
network. */