// The client side supervisor communicates over a network with the
// server side supervisor.
package supervisor

// sendCommand: Sends a command to the server and returns its response.

// updateWorkerStatus: Updates the worker's status.

// updateJobStatus: Updates the job's status.
